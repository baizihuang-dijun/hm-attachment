请先只排查，不要修改任何文件。

任务背景：后续要把网站内所有产品型号/系列里的 "YA" 前缀改成 "HMA"（例如 YA-123 改成 HMA-123，YADxxx 改成 HMADxxx 之类）。但只改型号/系列编号和 title 等文本，绝不能改交互逻辑、组件行为、事件处理、className、变量名、import、文件路径、路由等。

请在 src/ 全目录排查（重点 src/app、src/components、以及任何产品数据文件）：

1. 用 grep 搜索所有可能的 YA 型号写法，包括但不限于：
   - 独立词 YA
   - YA 后接字母/数字/连字符的模式，如 YA-、YA\d、YAD、YAA、YAS、"YA、'YA、>YA
   - 大小写变体（ya / Ya）
2. 对每一处出现，列出：文件路径、行号、该行完整内容、该处 YA 的具体含义判断（是"产品型号/系列名"还是"普通英文单词/变量/路径/其他"）。
3. 特别标注可能误伤的情况：任何 YA 出现在普通英文单词里（如 yawn）、变量名、className、import 路径、图片文件名/路径、URL/路由、API 里的，都要单独列出来并标记"不可替换"。
4. 汇总一个去重后的"型号/系列写法清单"（例如 [YA-100, YA-200, YAD...]），并说明每种写法建议替换成什么（YA→HMA 的对应结果）。
5. 检查 title 相关：页面 <title>/metadata（generateMetadata、metadata export）、产品卡片标题、H1/H2/规格表里的型号，分别在哪些文件。

只汇报，先不要改代码，也不要部署。