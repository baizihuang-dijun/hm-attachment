任务：把网站页眉（header/navbar）和页脚（footer）两处的品牌 logo，统一替换成我提供的同一张完整「HM Attachment」logo 图片。这两处目前的实现是「一个 logo 图形 + 后面跟一段文字（HM Attachment 之类）」分开拼的；现在要改成直接使用这一整张完整 logo 图（图形+ATTACHMENT 文字已在图内），不要再在图旁边/下面重复渲染原来的文字。

素材：随消息附件 hm-attachment-logo.png（2048×682，透明背景，宽高比约 3:1）。

请先排查再改：
1. 全仓找出 logo 的实现位置（很可能在 header / navbar 组件和 footer 组件，也可能有一个共用的 Logo/Brand 组件被多处引用）。列出每处所在文件、当前用的图片资源路径、当前文字是怎么渲染的（图标组件 + 文本 / 独立 <Image> 等），以及容器的高度约束。

2. 把附件 logo 放进项目 public 目录（建议 public/brand/hm-attachment-logo.png 或与现有 logo 同目录，命名清晰），在两处用 next/image（或站点现有图片用法）引用。

3. 替换要求：
   - 用整张完整 logo 替换原「图标+文字」组合；原来单独写的品牌文字（以及如仅是为该组合服务的旧小图标）应移除，避免出现重复的 HM Attachment 字样。若旧 logo 图片在别处仍被使用则保留文件，不要误删其它用途。
   - 按各位置容器高度等比缩放，保持 3:1 比例不变形；页眉一般较矮（按现有 header 高度，必要时 h-8/h-9 之类并 w-auto），页脚可略大，做到视觉协调、四周留白合适、垂直居中对齐。
   - logo 保持原有的链接行为（通常点击回首页 /）和 alt 文本（如 "HM Attachment"），不要改动任何路由、交互、组件逻辑。
   - 深色/浅色背景上都要清晰（logo 本身是黑红配色、透明底）；若某一处背景是深色导致黑色字看不清，请先如实告诉我该处背景色，不要自行改 logo 颜色。

4. 改完后：pnpm lint、ts/类型检查、生产构建（本项目为静态导出 output:export）都要通过；并确认 next build 静态产物中 logo 图片被正确复制、首页 HTML 引用的是新图。

汇报：两处（及是否还有第三处）logo 的文件与改动方式、新图存放路径、设置的显示尺寸、移除了哪些旧文字/旧引用、构建结果。先不要部署（GitHub Pages），改完等我确认。